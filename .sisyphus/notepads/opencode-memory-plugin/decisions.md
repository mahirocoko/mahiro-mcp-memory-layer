- 2026-04-09: Keep the standard OpenCode install path package-name based (`mahiro-mcp-memory-layer` in `opencode.json`) and leave the MCP CLI/server path as the advanced override. Preserve the existing MCP tool names and backend facade behavior unchanged.
- 2026-04-09: F2 code-quality review verdict = REJECT. Verification passed (`lsp_diagnostics` clean for `src/features/opencode-plugin`, `src/features/memory`, and `tests`; `bun run typecheck`; `bun run test`; `bun run build`), but approval is blocked until the plugin tool stops falling back to another session's cache, `message.updated`/`session.idle` stop reusing stale conversation across new turn ids, and the published package is trimmed to the intended plugin-first surface.
- 2026-04-09: Final-wave blocker resolution: `memory_context` now requires an explicitly resolved session id, empty-text new turns no longer inherit prior `recentConversation`, and the published package was trimmed to the plugin runtime plus shared memory core (`npm pack --json --dry-run` now reports `entryCount: 36`). The standard install story is explicitly plugin-only with env-var overrides, while standalone MCP use is documented as a separate manual fallback.
