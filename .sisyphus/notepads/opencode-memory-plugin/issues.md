- 2026-04-09: No official fail-open guarantee found in the OpenCode plugin docs. The page documents optional hooks, sequential hook execution, and blocking via thrown errors in examples, but it does not promise how plugin failures are isolated or recovered. Source: https://opencode.ai/docs/plugins/
- 2026-04-09: Repo search shows the plugin shell still lacks runtime-local debounce execution, per-session or per-message in-flight suppression, explicit event-type routing for `message.updated` / `session.idle`, and plugin-specific observability state. Current plugin code (`src/features/opencode-plugin/index.ts`, `src/features/opencode-plugin/runtime-shell.ts`) only forwards events into a singleton-backed readiness shell and log call, so these behaviors will need new local state if task 4/hook work requires them.
- 2026-04-09: Task 9 review found the published npm tarball is still broader than the plugin-first contract. `package.json` exports only `.` to `src/features/opencode-plugin/index.ts`, but `files: ["src", ...]` causes `npm pack --json --dry-run` to include 99 entries across unrelated CLI/orchestration sources. The plugin-first install path is therefore not minimally packaged yet.
- 2026-04-09: Task 9 review also found `getOpenCodePluginConfig()` exposes install/package metadata (`packageName`, `install.*`, `env`) that the runtime does not consume outside `tests/opencode-plugin-config.test.ts`; only `runtime.messageDebounceMs` is used by `src/features/opencode-plugin/index.ts`. That makes part of the new config test metadata-locking rather than behavior-backed parity.
- 2026-04-09: Manual QA for task 9 still leaves one packaging caveat: `package.json` exports the plugin from raw `src/features/opencode-plugin/index.ts`, so the plugin-first install path depends on the OpenCode/Bun loader accepting TypeScript package exports; the strongest behavior suite also imports `../src/features/opencode-plugin/index.js` rather than the package-name root.
- 2026-04-09: F1 audit for plan task 9 rejected the docs/package wave on one remaining mismatch: `package.json` and `tests/opencode-plugin-package.test.ts` do prove the package-root plugin load path and MCP parity surface, but `README.md` still labels the standalone MCP server as the "Advanced override path" instead of explicitly documenting the v1 environment-variable-based plugin override path separately from the non-plugin MCP fallback. That leaves the install/migration story more ambiguous than task 9 allows.
- 2026-04-09: F2 code-quality review rejects the current plugin/runtime wave on three maintainability bugs despite green checks. (1) `src/features/opencode-plugin/runtime-shell.ts:490-496` makes `memory_context` fall back to `runtimeState.latestSessionId` when the tool context does not carry a session id, so the model-facing tool can expose cached state from the wrong session instead of failing closed/empty. (2) `src/features/opencode-plugin/runtime-shell.ts:574-578` preserves `existingState?.recentConversation` for every new `message.updated` event whose payload lacks extractable text, while `session.idle` persists by the new `lastMessageId`/`messageVersion` key at `runtime-shell.ts:342-374`; that can write stale prior-turn conversation under a fresh turn key. (3) `npm pack --json --dry-run` still reports `entryCount: 99` and includes unrelated cursor/gemini/orchestration sources even though the plugin-facing export is only `src/features/opencode-plugin/index.ts`, so the shipped plugin surface is not actually narrow yet.
- 2026-04-09: Remaining non-blocking caveat after the blocker fix: the package root still exports raw TypeScript (`src/features/opencode-plugin/index.ts`), so the plugin-first install path still assumes the OpenCode/Bun loader accepts TypeScript package exports. That matches the current OpenCode/npm+Bun install story, but it remains a loader assumption rather than a separately built JS artifact.
