- 2026-04-09: The plugin contract suite targets `src/features/opencode-plugin/index.ts` as an OpenCode plugin module that exports a documented `server` function, matching the upstream `PluginModule` shape instead of inventing a wrapper-style entrypoint.
- 2026-04-09: The contract enforces no self-spawn at the plugin path with a mocked `node:child_process` spawn plus shell/Bun guards, and it checks wrapper parity by validating plugin-triggered payloads against `wakeUpMemoryInputObjectSchema` and `prepareHostTurnMemoryInputObjectSchema` rather than hard-coding new plugin-only payload shapes.
- 2026-04-09: A safe extraction seam for the future plugin path is a transport-free `memory-facade` that depends only on shared `buildContext` and `applyConservativeMemoryPolicy` functions; `MemoryService` can delegate wrapper methods to it while MCP/plugin adapters stay responsible only for transport registration and event wiring.
- 2026-04-09: Repo-native config style stays small and feature-local: prefer a dedicated `src/features/.../config.ts` or `resolve-*.ts` helper with injected `env` plus one focused top-level `tests/*.test.ts`, rather than introducing a new central env loader for plugin-only overrides.
- 2026-04-09: OpenCode's public plugin docs expose stable project/worktree/directory and event `sessionID`, but not a documented user/account identifier. A safe scope rule is: resolve `projectId` from `project.id` then `project.name`, resolve `containerId` from `worktree:${path.resolve(worktree)}` then `directory:${path.resolve(directory)}`, resolve `sessionId` from `properties.sessionID` then `properties.info.id`, and leave scope deliberately `incomplete_scope_ids` unless the runtime can provide a stable explicit `userId`.
- 2026-04-09: The plugin runtime singleton must live in a stable module instance. In tests, `vi.resetModules()` between two `server()` constructions will falsely make a singleton look broken, so singleton assertions need one shared import and a dedicated reset helper only in teardown.
- 2026-04-09: Existing stateful plugin/runtime reuse points are narrow and explicit: `src/features/opencode-plugin/runtime-shell.ts` has the only plugin-local singleton (`singletonMemoryBackendPromise` via `getOpenCodePluginMemoryBackend()` / `getOrCreateSingletonMemoryBackend()`), `src/features/opencode-plugin/config.ts` exposes `runtime.messageDebounceMs` as config-only input, and `src/features/opencode-plugin/resolve-scope.ts` already resolves event session scope from `event.properties.sessionID` or `event.properties.info.id` while preserving `incomplete_scope_ids` instead of guessing.
- 2026-04-09: Existing repo patterns to reuse for hook statefulness live outside the plugin shell: `src/features/memory/core/upsert-document.ts` is the clearest idempotent-write model (scope + source identity, replace-in-log, reindex), `src/features/memory/core/apply-conservative-memory-policy.ts` is the existing incomplete-scope gate, `src/features/memory/memory-facade.ts` is the reusable session-start/turn wrapper layer, `src/features/memory/retrieval/hybrid-search.ts` is the strongest degrade-but-continue path, and `src/features/memory/observability/retrieval-trace.ts` plus orchestration trace/result stores provide the existing append-only trace vs mutable latest-state split.
- 2026-04-09: The strongest existing tests for upcoming hook/runtime-shell work are `tests/product-memory-plugin.test.ts` (behavior-light event shell, no self-spawn, singleton reuse across plugin server instances), `tests/opencode-plugin-scope-resolution.test.ts` (session/project/container fallback resolution), `tests/opencode-plugin-config.test.ts` (debounce env parsing only), `tests/gemini-worker.test.ts` (cache hit/version/expiry behavior), `tests/run-worker-job.test.ts` (retry/backoff and runner failure behavior), `tests/memory-service.test.ts` plus `tests/hybrid-search.test.ts` (idempotency and degraded fallback), and `tests/mcp-stdio-worker-e2e.test.ts` (stdio/MCP runtime boundary).
- 2026-04-09: Official OpenCode plugin docs say a plugin is a JavaScript/TypeScript module that exports one or more plugin functions, each receiving a context object and returning a hooks object. Local plugins load from `.opencode/plugins/` and `~/.config/opencode/plugins/`, while npm plugins are listed in `opencode.json` and installed with Bun at startup. Source: https://opencode.ai/docs/plugins/
- 2026-04-09: Documented hook names on the official plugins page include `event`, `command.executed`, `file.edited`, `file.watcher.updated`, `installation.updated`, `lsp.client.diagnostics`, `lsp.updated`, `message.updated`, `message.removed`, `message.part.updated`, `permission.asked`, `permission.replied`, `server.connected`, `session.created`, `session.compacted`, `session.deleted`, `session.diff`, `session.error`, `session.idle`, `session.status`, `session.updated`, `todo.updated`, `shell.env`, `tool.execute.before`, `tool.execute.after`, `tui.prompt.append`, `tui.command.execute`, and `tui.toast.show`. The docs only guarantee the event names, not payload internals. Source: https://opencode.ai/docs/plugins/
- 2026-04-09: The official compaction example says `experimental.session.compacting` fires before the LLM generates a continuation summary. The docs show two supported behaviors, append extra context with `output.context.push(...)`, or replace the default continuation prompt entirely by setting `output.prompt`, which also ignores `output.context`. Source: https://opencode.ai/docs/plugins/
- 2026-04-09: Official docs also state that custom tools are added through the `tool` helper from `@opencode-ai/plugin`, plugin tools are available alongside built-in tools, and a plugin tool with the same name as a built-in tool takes precedence. The `shell.env` hook can inject environment variables into shell execution. Source: https://opencode.ai/docs/plugins/
- 2026-04-09: Public example material shows session-scoped plugin state should live in a Map keyed by session ID, with cleanup on `session.created` and `session.deleted`. That is a community pattern, not an official guarantee. Sources: https://gist.github.com/CypherpunkSamurai/30dc0b7683c06560a74f783097c5f912 and https://github.com/bristena-op/opencode-session-handoff
- 2026-04-09: I did not find an official fail-open guarantee on the plugin page. The docs do say all hooks are optional and all hooks from all plugins run in sequence, and the examples show hooks throwing to block tool execution. Treat fail-open persistence as an implementation choice, not a documented contract. Source: https://opencode.ai/docs/plugins/
- 2026-04-09: In this Bun 1.3.11 + Vitest environment, `vi.mock("node:child_process", async (importOriginal) => ...)`, `vi.resetModules()`, and `vi.mocked(...)` are not available as in full Vitest. The compatible pattern in `tests/product-memory-plugin.test.ts` is a plain factory mock returning `spawn: vi.fn(...)`, plus direct mock-instance casts and an optional guard around `resetModules`.
- 2026-04-09: The OpenCode plugin runtime shell needs module-scoped singleton state for both the lazy memory backend and the per-session cache map. Keeping only the backend promise singleton is not enough, because the native `memory_context` tool must be able to read the same cached session state across separate `server()` constructions in one process.
- 2026-04-09: `session.created` can be delivered through both the generic `event` hook and the dedicated `"session.created"` hook. Session-start wake-up must dedupe by `sessionId` inside the singleton runtime state (for example with a started/pending marker) rather than by hook name, or duplicate delivery will re-run backend wake-up work.
- 2026-04-09: Debounced `message.updated` precompute needs two guards, not one: replace the prior session timer so repeated updates collapse, and also compare the completion against the latest `messageVersion` before caching. Clearing a timeout alone does not stop an already-started older async precompute from finishing late and overwriting newer cached turn context.
- 2026-04-09: Idle-time duplicate-turn protection should key off `lastMessageId` first and only fall back to `messageVersion` when no message id exists. In tests and fixtures, a “new turn” therefore needs a distinct `messageID`; changing only the message text still represents the same handled turn to the plugin runtime.
- 2026-04-09: Best-effort `experimental.session.compacting` continuity should only append cached context when the host output supports append semantics and `output.prompt` is still unset. If a prompt is already present, treat continuity as degraded and log that outcome rather than forcing extra context into a path the host will ignore.
- 2026-04-09: The package root can be used as the OpenCode plugin entry once `package.json` exports `src/features/opencode-plugin/index.ts`; a self-import by package name is a useful test for the plugin-first install path, while the MCP server can stay on the existing CLI path.
- 2026-04-09: Manual QA for task 9 confirmed the user-facing install story is plugin-first in both `README.md` and `AGENTS.md`, and a concrete `bun` package-root import resolves `mahiro-mcp-memory-layer` with a callable `server` export inside this repo.
- 2026-04-09: Scope review for task 9 stayed clean: `package.json` only repoints the package root to the plugin module, `README.md`/`AGENTS.md` make the standard path plugin-only with MCP clearly labeled advanced/manual, and `tests/opencode-plugin-package.test.ts` adds package-root export plus fixed-input plugin/MCP parity assertions without introducing new runtime requirements.
- 2026-04-09: The safe `memory_context` read rule is strict session affinity: resolve `sessionID` from the tool context only, and if none is present return the empty shape with `latestSessionId` as metadata rather than silently reading another session's cache.
- 2026-04-09: For `message.updated`, preserving `recentConversation` is only safe when the event is still for the same `messageID`; if a new turn arrives without extractable text, clear the conversation state so `session.idle` cannot persist stale text under the fresh turn key.
- 2026-04-09: Narrowing `package.json#files` to `src/config`, the shared memory runtime paths, and `src/features/opencode-plugin` trimmed `npm pack --json --dry-run` from 99 entries (pre-fix review finding) to 36 entries while keeping the plugin-root import working.
