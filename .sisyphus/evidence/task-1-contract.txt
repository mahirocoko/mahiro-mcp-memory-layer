# Task 1 wiki materializer contract evidence

- Plan item verified: `- [ ] 1. Define wiki materializer contract and file layout`.
- Contract module: `src/features/memory/wiki-materializer/contracts.ts`.
- Targeted test file: `tests/wiki-materializer-contract.test.ts`.
- Contract coverage: schema version `1`, materializer options, selected record shape, manifest shape, generated page shape, output layout constants, filter modes/flags, and exclusion reasons.
- Default layout coverage: `.agent-state/wiki/<projectSlug>/<containerSlug>/` under `paths.appRoot`; `outputDir` is only a CLI/test final scope directory override in the options contract.
- Boundary coverage: wiki materializer remains internal and CLI-oriented; MCP/no-tool exposure evidence is recorded separately in `.sisyphus/evidence/task-1-no-mcp-tool.txt`.

## Verification summary

- `lsp_diagnostics` on `src/features/memory/wiki-materializer/contracts.ts`: no diagnostics.
- `lsp_diagnostics` on `tests/wiki-materializer-contract.test.ts`: no diagnostics.
- `rtk bun run test tests/wiki-materializer-contract.test.ts`: PASS, 1 test file, 4 tests.
- `rtk bun run typecheck`: PASS, ran `tsc -p tsconfig.json --noEmit`.
- Extra verification: `rtk bun run build`: PASS, ran `tsc -p tsconfig.json`.
