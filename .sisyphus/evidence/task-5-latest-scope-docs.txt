MCP_USAGE.md:96:- `inspect_memory_retrieval` exposes `requestId` as the public input. The plugin path may inject scoped latest lookup state internally when no `requestId` is supplied, but `latestScopeFilter` is not a user-facing input.
MCP_USAGE.md:100:- When the plugin path returns an empty scoped latest lookup with `latestScopeFilter`, the scoped trace lookup missed for that `projectId`/`containerId`; it does not prove the global trace store is empty.
CONTINUITY_DEBUGGING.md:52:`inspect_memory_retrieval` exposes `requestId` only as public input. When the plugin path runs without a `requestId`, it may inject the active session scope internally and return `latestScopeFilter` as diagnostic metadata on an empty scoped latest lookup. That metadata is not a public input contract.
CONTINUITY_DEBUGGING.md:54:When a latest scoped lookup is empty, the attempted `latestScopeFilter` shows that no matching trace was found for that scope, not that the global trace store is empty.
ARCHITECTURE.md:122:`inspect_memory_retrieval` รับ public input เป็น `requestId` เท่านั้น. ถ้า plugin path เรียกแบบไม่มี `requestId`, runtime อาจ inject latest scoped lookup metadata ภายในและส่ง `latestScopeFilter` ออกมาเป็น diagnostic metadata เมื่อ scoped latest lookup empty. `latestScopeFilter` จึงไม่ใช่ public input contract.
